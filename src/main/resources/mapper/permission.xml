<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace 理论上随意命名 因为mybatis动态代理的使用 书写时，要讲namespace指定为接口的全路径 -->
<mapper namespace="com.vivi.bugTracking.dao.mapper.PermissionMapper">
    <resultMap id="permissionResultMap" type="Permission">
        <id property="id" column="id"/>
        <result property="resourceName" column="resource_name"/>
        <result property="actionName" column="action_name"/>
    </resultMap>
    <select id="selectPermissionByRoleId" resultMap="permissionResultMap" parameterType="int">
        select p.permission_id as id, resource_name, action_name
        from v_permission p
                 inner join role_permission rp on p.permission_id = rp.permission_id
        where rp.role_id = #{roleId}
    </select>

</mapper>